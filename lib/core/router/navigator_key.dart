import 'package:flutter/widgets.dart';

/// Global key to access the NavigatorState from outside the widget tree
/// (e.g. from the MaterialApp.builder overlay in app.dart).
final navigatorKey = GlobalKey<NavigatorState>();
